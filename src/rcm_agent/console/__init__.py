"""The operator console: a web UI over what a run recorded.

Orchestrator-only, like `agent` and `browser`. The sandbox serves the mocks and
runs the analysis kernel; it has no screen and is excluded from the archive that
ships to it.
"""
