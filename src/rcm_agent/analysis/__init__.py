"""Code that runs inside the Sandbox.

Uploaded from the working copy at run start rather than baked into an image, and
written as plain functions over paths so it is unit-testable locally without a
live sandbox in the loop.
"""
